language: node_js
node_js: stable

install:
  - npm install -g hexo
  - npm install -g hexo-cli
  - npm install

cache:
  directories:
    - "node_modules"

before_script:
  - git clone --branch=master https://github.com/zhanghTK/zhanghTK.github.io.git public

script:
  - hexo clean
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "zahnghTK"
  - git config user.email "510223064@qq.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${GH_TOKEN}@${GH_REF}" master:master

branches:
  only:
    - hexo
env:
 global:
   - GH_REF: github.com/zhanghTK/zhanghTK.github.io.git
